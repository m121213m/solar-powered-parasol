# Images directory

Place your storefront images in this folder so they can be referenced with paths like `assets/images/hero.jpg` or `assets/images/parasol-card.jpg`.

> Tip: keep filenames simple and update the paths in `index.html` and `assets/js/main.js` if you change them.
